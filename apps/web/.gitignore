.cf-home/
.open-next/
.wrangler/
# cloudflare-env.d.ts is generated by `./cf npm run cf-typegen` but committed:
# it is what makes env.DB and env.ARTIFACTS type-check, so a checkout
# must not need the toolchain to run before tsc will pass.
