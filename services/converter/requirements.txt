# Everything: the service, plus the local OCR engine.
#
# This is what a developer or an editor installs — the CLI still reads
# scanned PDFs locally (`app/ocr`, `pipeline/structure.py`), which is the
# path that reconstructs a book from OCR *geometry* and finds headings,
# verse and footnotes from where a line sits on the page.
#
# The deployed service installs `requirements-service.txt` instead. Books
# arriving through the portal are read by Google Document AI and reach
# this service as text, so the container needs none of the below.
-r requirements-service.txt

# OCR. paddlepaddle is the runtime; paddleocr is the model pipeline.
# See app/ocr/paddle.py for why oneDNN is disabled by default.
paddlepaddle>=3.3
paddleocr>=3.7
